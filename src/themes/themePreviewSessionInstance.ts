import { appearanceRuntime } from "./appearanceRuntimeInstance";
import { ThemePreviewSession } from "./ThemePreviewSession";

export const themePreviewSession = new ThemePreviewSession(appearanceRuntime);
